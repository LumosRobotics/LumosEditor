 * Configure custom commands through file, e.g. vel 2.0
 * Real time plotting
 * Sensor data viewer with pre built firmware that reads data from sensors and sends to serial
 * IPC interface for external tools to query the editor to close the serial port, reopen it, etc.